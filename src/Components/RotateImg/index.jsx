import * as Styled from './styles';
import { FaReact } from 'react-icons/fa';

export const RotateImage = () => {
  return (
    <Styled.Container>
      <FaReact size={300} />
    </Styled.Container>
  );
};
