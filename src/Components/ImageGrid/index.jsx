import * as Styled from './styles';
import { FaReact } from 'react-icons/fa';

export const ImageGrid = () => {
  return (
    <Styled.Container>
      <FaReact size={300} />
    </Styled.Container>
  );
};
